let dataSearch = "";
export const dataSearchReducer = (state = dataSearch, action) => {
  switch (action.type) {
    case "CHANGE":
      defatul:
      return state;
  }
  state = action.dataSearch;
}

